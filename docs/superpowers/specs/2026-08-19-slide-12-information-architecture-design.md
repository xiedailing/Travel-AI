# Slide 12 — Information Architecture

## Goal

Make the existing product-structure slide read unmistakably as Trova's information architecture without adding a redundant slide.

## Content

- Eyebrow: `Information Architecture`
- Title: `從收藏到行程，資訊如何被組織`
- Keep the three core modules as the primary horizontal path: 收藏 → AI 行程安排助手 → 行程.
- Preserve the existing module descriptions and outputs because they explain how information changes across the system.
- Add two visually secondary global entries: 探索（搜尋景點、景點詳情） and 個人（偏好與設定）.
- Exclude all candidate/backup itinerary functionality.

## Layout

The three core modules remain the largest elements. A compact secondary-navigation row sits above them, so 探索 and 個人 are identifiable as supporting destinations rather than extra steps in the core flow. The bottom product-logic statement remains as the takeaway.

## Responsive behavior

On narrow screens, the secondary entries stay compact and the existing core-module layout continues using the slide's current mobile rules. No new page is inserted, so later slide numbering remains unchanged.
